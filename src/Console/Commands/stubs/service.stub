<?php

namespace {namespace};

use L0n3ly\LaravelRepositoryWithService\Services\Service;
use {repositoryInterfaceNamespace}\{repositoryInterfaceName};

class {serviceName} extends Service implements {serviceInterface}{

     /**
     * don't change $this->mainRepository variable name
     * because used in extends service class
     */
     protected {repositoryInterfaceName} $mainRepository;

    public function __construct({repositoryInterfaceName} $mainRepository)
    {
      $this->mainRepository = $mainRepository;
    }

    // Define your custom methods :)
}
